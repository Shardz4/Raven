# x402 Payment Gateway Configuration
# Defaults to https://x402.pay/invoice if not set
# Change this to your own x402 gateway URL or use a different payment service
X402_GATEWAY_URL=https://x402.pay/invoice

# Raven miner network mode (default: Cortensor live)
RAVEN_MINER_MODE=cortensor
# RAVEN_MINER_MODE=production   # real LLMs: OpenAI/Claude/DeepSeek/Grok/Ollama (keys below)

# Workflow tuning
RAVEN_REDUNDANCY=3
DOCKER_TIMEOUT=30

# Payments
# X402_MODE=production          # call real x402 gateway instead of demo auto-approve

# --- Real Cortensor network (when RAVEN_MINER_MODE=cortensor) ---
# Router Node base URL (see https://docs.cortensor.network/getting-started/web2-api-reference)
CORTENSOR_ROUTER_URL=http://localhost:5010
CORTENSOR_API_KEY=default-dev-token
# CORTENSOR_SESSION_ID=0

# --- Optional: LLM API keys (when RAVEN_MINER_MODE=production) ---
# OPENAI_API_KEY=your_openai_key
# ANTHROPIC_API_KEY=your_anthropic_key
# DEEPSEEK_API_KEY=your_deepseek_key
# XAI_API_KEY=your_xai_grok_key

# --- Optional: Bot tokens (Telegram / Discord) ---
# TELEGRAM_BOT_TOKEN=...
# DISCORD_BOT_TOKEN=...
